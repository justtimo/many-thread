package com.wby.thread.manythread.Chapetor10内部类.node12内部类标识符;

/**
 * @Description:
 *  由于每个类都会产生.class文件，其中包含了如何创建该类型对象的全部信息（此信息产生一个“meta-class”，叫做Class对象），你可能猜到了，内部类也必须生成一个.class文件以包含他们的对象信息
 *  这些类文件的命名有严格的规范：外围类的名字上，加上“$”，再加以内部类的名字。
 *  例如，LocalInnerClass.java生成的.class文件包括：
 *      Counter.class
 *      LocalInnerClass$1.class
 *      LocalInnerClass$1LocalCounter.class
 *      LocalInnerClass.class
 *  如果内部类是匿名的。，编译器会简单地产生一个数字作为其标识符。
 *  如果内部类是嵌套在其他内部类里面，只需要将他们的名字加在其外围类标识符与“$”的后面
 *
 */
public class Text {
}
